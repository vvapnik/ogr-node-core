; Host-based unit tests for the platform-independent protocol engine.
; Real hardware targets live in the ogr-node-hal-* packages, which depend on
; this library instead of building it standalone.
[env:native]
platform = native
test_framework = unity
test_build_src = yes
build_flags = -std=gnu++14 -Wall -Wextra
